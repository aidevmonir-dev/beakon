# Tunnel demo for Thomas — points the browser at the Django tunnel URL.
# (Tunnel expired; commented out so it falls back to http://localhost:8000/api/v1.)
# NEXT_PUBLIC_API_BASE=https://felt-boolean-belong-ribbon.trycloudflare.com/api/v1
